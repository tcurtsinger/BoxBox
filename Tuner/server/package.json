{
  "name": "boxbox-tuner-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BoxBox Tuner ingest: reads the player car's setup + telemetry (F1 25 / 2026 pack)",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  }
}
