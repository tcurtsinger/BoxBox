{
  "name": "boxbox-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "BoxBox telemetry ingest + parser (F1 26 / 2026 season pack)",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0"
  }
}
